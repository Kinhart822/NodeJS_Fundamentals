<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upload File</title>
    <link rel="stylesheet" href="/css/style.css" />
    <style>
        .error-message {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #f5c6cb;
            border-radius: 4px;
            text-align: center;
        }
        .image-gallery {
            margin-top: 20px;
        }
        .image-gallery img {
            max-width: 500px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
    </style>
</head>

<body>
    <div class="topnav">
        <a href="/">Home</a>
        <a href="/upload" class="active">Upload File</a>
    </div>

    <div class="container">
        <h2>📤 Upload a File</h2>

        <% if (error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <form action="/upload-profile-pic" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label>Select your profile picture (max 5MB):</label>
                <input type="file" name="profile_pic" accept="image/*" />
            </div>
            <div>
                <button type="submit" class="btn btn-submit">Upload</button>
            </div>
        </form>

        <form action="/upload-multiple-images" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label>Select multiple images (max 10 images, 5MB each):</label>
                <input type="file" name="multiple_images" multiple accept="image/*" />
            </div>
            <div>
                <button type="submit" class="btn btn-submit">Upload</button>
            </div>
        </form>

        <% if (images && images.length > 0) { %>
            <div class="image-gallery">
                <h3>Uploaded Images</h3>
                <% images.forEach(image => { %>
                    <img src="<%= image.src %>" alt="<%= image.alt %>" />
                <% }) %>
                <a href="/upload">Upload more images</a>
            </div>
        <% } %>
    </div>
</body>

</html>